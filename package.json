{
  "name": "hive-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix app\" \"npm start --prefix server\"",
    "build": "npm run build --prefix app && npm run build --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
